import { Text, View } from "react-native";
import { ui } from "../utils/styles";

export default function SummaryHero() {

    return (
        <View style={[ui.flex]}>
            <Text>Summary hero</Text>
        </View>
    )
}